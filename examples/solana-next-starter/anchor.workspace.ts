// Minimal anchor-workspace stub for the Anchor TypeScript SDK.
// When you run `anchor test` or `anchor run`, Anchor looks for this file
// to resolve program handles. Replace `MyProgram` with the Codama/IDL-derived
// type once you've run `polyq codegen`.
//
// See https://www.anchor-lang.com/docs/references/anchorpy-workspace for the
// full shape. For the `polyq dev` flow you usually don't need this — it's
// here so `anchor test` works out of the box.
export {}
